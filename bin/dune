(executable
 (public_name cadenza)
 (name mm)
 (libraries
  cadenza
  lwt
  yojson
  uri
  lwt.unix 
  ipaddr
  ipaddr.unix
  websocket-lwt-unix
  mirage-crypto-rng-lwt
  cohttp
  conduit-lwt-unix
  conduit-lwt
  sexplib
  tls-lwt
  tls
  fmt.tty)
 (foreign_stubs
  (language c)
  (names ring_consumer_stub))             ; will compile ring_consumer_stub.c
 ;; If ring_mmap is a system/shared lib you built, link it:
 (link_flags -cclib -L/mnt/ntfs/Code/aether/build -cclib -lring_mmap)  ; adjust path or install libring_mmap system-wide
 (preprocess (pps lwt_ppx ppx_jane)))
;;  export LD_LIBRARY_PATH=/mnt/ntfs/Code/aether/build:$LD_LIBRARY_PATH before dune exec bin/mm.exe
