(executable
 (public_name cadenza)
 (name bb)
 (libraries
  cadenza
  lwt
  yojson
  uri
  lwt.unix 
  ipaddr
  ipaddr.unix
  websocket-lwt-unix
  mirage-crypto-rng-lwt
  cohttp
  conduit-lwt-unix
  conduit-lwt
  sexplib
  tls-lwt
  tls
  fmt.tty)
 (foreign_stubs
  (language c)
  (names ring_consumer_stub))             ; will compile ring_consumer_stub.c
 ;; If ring_mmap is a system/shared lib you built, link it:
 (link_flags -L/mnt/ntfs/Code/aether/build -lring_mmap)  ; adjust path or install libring_mmap system-wide
 (preprocess (pps lwt_ppx ppx_jane)))
